    create table users (
        id int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
        firstName varchar(100),
        surnames varchar (500),
        course varchar(300),
        puntuaction int(11)

    );

  